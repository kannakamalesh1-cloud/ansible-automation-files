- name: Copy HTML file to webservers
  hosts: webservers
  become: yes
  tasks:
    - name: Copy index.html to /var/www/html
      ansible.builtin.copy:
        src: files/index.html
        dest: /var/www/html/index.html
        owner: root
        group: root
        mode: '0644'
